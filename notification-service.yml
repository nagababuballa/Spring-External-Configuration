server:
  port: 3105
spring:
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: paymentGroup,orderGroup
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: '*'
        spring.json.type.mapping: orderConfirmation:com.realtime.notification.kafka.notification.OrderConfirmation,paymentConfirmation:com.realtime.notification.kafka.notification
  data:
    mongodb:
      uri: mongodb+srv://nagababu:nagababu@cluster0.aso3j.mongodb.net/payment?retryWrites=true&w=majority
  mail:
    host: localhost
    port: 1025
    username: nagababu
    password: nagababu
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enabled: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000
